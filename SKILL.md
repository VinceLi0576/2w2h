---
name: 2w2h
description: 把一份 md 正本出成黄金圈 HTML（TL;DR ＋ WHY/HOW/WHAT/HOW-GOOD 四档 ＋ 折叠段 ＋ 页内搜索）。当作者说「出页」「给这份 md 出个 html」「做个页面」「按黄金圈出」「重出 HTML」时触发。走本夹共用出页器 build_ring，🚫 不手写 HTML。
---

# 2w2h —— 黄金圈出页（2W ＝ WHY · WHAT，2H ＝ HOW · HOW GOOD）

📌 **这个文件夹是什么**：2w2h 出页 skill 的**正本源头** —— 装在云主机 `~/.claude/skills/2w2h/`，软链接到 `~/.claude/skills/2w2h/` 供全机调用。github 上 `VinceLi0576/2w2h` 是从这儿清洗出去的**派生**（少了自述页／批注／发布工具）。🔴 **别删、别当没用的**：几十个生成器 import 它的 `_出页.py`，删了全断；它也是同步 github 的唯一源头。你本人不用直接翻它，看站上的自述页或 github 就行 —— 这文件夹是给系统用的。

🔴 **本夹是全机 HTML 格式标准的真身**。
**`~/projects/<旧路径>` 现在是软链接**，指到本夹 —— 全机各项目的生成器写死的还是那条旧路径，
一根软链接全接住，一处没改（搬家当天验过：走旧路径重出的页与搬家前**字节级一致**）。
⭐ **作者的计划：等这套完全跑顺了，再把各生成器改成直接路径、撤软链接。**
在那之前 🚫 别动软链接 · 🚫 别改**老**生成器的 `sys.path` · 🚫 别把本夹文件挪进 `scripts/`（挪了软链接就接不住）。
⭐ **新生成器一律走 `~/.claude/skills/2w2h`**（本机是软链接→本夹；别人 clone 到那儿就通）—— 这就是「撤软链接」那步的起点。

🌐 **公开库 <https://github.com/VinceLi0576/2w2h>**（作者  建）—— 本夹是正本，改完 `bash scripts/发布到github.sh "一句话"` 单向推；
不推 `归档/`、`_旧设计语言-待退役.css`（内部件）。🚫 别在公开库里直接改。

⚠️ **本 skill 删不得** —— `_出页.py` `_md2html.py` 是全机几十个生成器 import 的公共库，不是本 skill 私有跑手。
🚫 数字不写这儿（会变，且口径多：按 import 语句数 ≠ 按写死路径数），现采：
```bash
grep -rl 'from _出页 import\|from _md2html import' ~/projects --include=*.py | wc -l   # 真 import 的生成器
grep -rl '<旧路径>' ~/projects --include=*.py --include=*.md --include=*.sh | wc -l   # 写死旧路径的文件（含 md）
```

## 本 skill 管什么、不管什么

- ✅ 管「**这次这页怎么落地**」：六步 · 四档怎么判 · 五个坑 · 验收
- 🚫 不管「标准长什么样」—— 同夹 `结构输出标准.md`（必备五件 · 机读标记 · 反例六条 · 配色），用到就 `cat`，本文件不抄第二份
- 🚫 不管三层管线的来历与归属 —— 同夹 `说明-三层管线与归属.md`
- 📌 只走「md 正本 → 生成器」这条路。`cp 模板.html` 手写那条路**缺页内搜索／稳定锚／动态 bar**（说明 md 里实测过），本 skill 不覆盖它

## 六步

### ① 先有 md 正本

没有就先立 md。🔴 立文件前照 ops 规矩**先问一句**。

### ② md 里打机读标记

每个 `## 标题` 的**下一行**放一条：

```
<!-- sec tldr sub=整页就讲这一件事 -->
<!-- sec tag=WHY sub=副标一句 ring=按钮标题|一句钩子 key=稳定锚 -->
<!-- sec sub=各摊正本在哪 -->
```

- 第 1 段必是 `tldr`，正文是**三条**有序列表 —— 它渲染进**抬头**（标题 → 一句话 → 三行 → 功能栏），不再是折叠段。
  🔴 字数：标题 ≤ 16 · 一句话（生成器 `lead=`）≤ 30 · 三行每行 ≤ 20（🔴 含「它是什么：」这类标签，标签占 5 字 ⇒ 内容实际约 15 字； 某条对话线 按内容 20 写、全超），超了 stderr 报。三行各答「它是什么 · 什么时候动 · 不对怎么改」，🚫 跟一句话重复
- 🔴 **页别太长**：每段正文只留那句结论 ＋ 几个 `###` 小节标题，原话／清单／引文／现采表全塞进 `###`（默认收起）。一段超过一屏就拆。验收会对平铺过多的段报 ⚠️
- 除第 0 段外所有段默认折叠；第 0 段给 `open`、展开当整页 TL;DR（打开先看见那三句，其余是段头）
- **段头收起时自动露正文第一句结论**⇒ 每段第一段必须是一句能独立成立的结论，🚫 别拿「先给结论：」之外的铺垫开头
- **每段都给 `key=`**（短、稳、不跟标题走）—— 没 key 的段只能靠会漂的 `#sN` 引用
- 倒数两段固定：`📎 资料库`（key=refs）＋ `📖 概念库`（key=terms），标题就俩字前面一个 emoji，**概念库 ≤ 20 条**（验收硬查）；骨架是**文档版**（去卡分隔线／左边线，非卡片）。旧写法：`📎 资料库 · 对照读物，不是作业` ＋ `📖 概念库 · 本页用到的词`（key=terms，一个词一句人话），都不挂档
- 语法细则 → `结构输出标准.md`「机读标记约定」节

### ③ 四档怎么判（唯一的真难点）

拿素材**一句一句问「这句回答的是哪个问题」**：

| 档 | 它回答 | 一句自测 | 最常见的错 |
|---|---|---|---|
| **WHY** | 为什么要管这事 | 删掉这段，读者还知道**不做会怎样**吗？ | 🔴 写成功能清单（那是 WHAT） |
| **HOW** | 凭什么做到 | 讲的是**机制／原理**，不是操作步骤？ | 写成命令清单（那是 WHAT） |
| **WHAT** | 具体是什么、有哪些 | 能 `ls`／列表出来的？ | 混进「为什么」的辩护 |
| **HOW GOOD** | 做到什么程度算好 | 有**验收标准 ＋ 今晚能落地的最小一步**吗？ | 🔴 整档漏掉 —— 没验收等于不知道做到没 |

- 一段横跨两档 ⇒ 按主句归档，或拆段。一档横跨两段是允许的
- 缺哪档，出页器会往 stderr 报 `🔴 黄金圈缺档` —— **缺档说明那部分没想清楚，🚫 别硬凑一段糊过去**

### ④ 写生成器（20 行）

放在 md 同夹，命名 `_生成<页名>HTML.py`：

```python
#!/usr/bin/env python3
"""<页名>.html 的生成器。骨架在 ~/.claude/skills/2w2h/_出页.py，🚫 别在这写第二份。"""
import os, sys, pathlib
sys.path.insert(0, os.path.expanduser("~/.claude/skills/2w2h"))   # 本机是软链接→ops/skill/2w2h；别人 clone 到这儿就通
from _出页 import build_ring
D = pathlib.Path(__file__).resolve.parent
build_ring(src=D / "<页名>.md", dst=D / "<页名>.html",
           eyebrow="ops · <摊>", lead="一句话说这页解决什么", version="v1",
           changelog=[("v1", "<日期>", "开张")],
           gen_rel="ops/<夹>/_生成<页名>HTML.py",
           inject={})   # 会变的数字在这儿现算，🔴 给 markdown
```

🔴 **五个坑**（全撞过，出处在各生成器注释里）：

1. **文件名 🚫 叫 `_出页.py`** —— 脚本自己的夹是 `sys.path[0]`，同名会 import 到自己，**静默导错**（`对齐/` `自动化/` 各撞一次）
2. **`inject=` 给 markdown，🚫 给 HTML** —— 注入发生在渲染之前，给 HTML 会被转义成字面量 `&lt;code&gt;`
3. **局部变量 🚫 叫 `parts`** —— 撞掉出页器外层同名变量，7 段正文全丢、98KB → 30KB，**Python 不报错**
4. **会变的数字（清单／数量／体积／版本）一律 `inject` 现算**，🚫 写进 md —— 手写的清单第二天就开始烂，烂得没有声音
5. **额度／价格／到期日只写「去哪查」**，🚫 写数值

### ⑤ 跑，看 stderr

```bash
cd <md 所在夹> && python3 _生成<页名>HTML.py
```

stdout 一行 `✅ xxx.html  NN KB · N 段 · 黄金圈 4 档`。stderr 四种警告，🔴 **一条都不许留**：

- `🔴 黄金圈缺档` → 回 ③
- `⚠️ 这些段没有稳定锚` → 补 `key=`
- `🔴 未填的注入占位` → 生成器 `inject=` 少传了
- `⚠️ 段首不是段落而是 <pre>` → 那段开头放了代码块／列表，先补一句结论再放

### ⑥ 验收

```bash
bash ~/.claude/skills/2w2h/scripts/验收.sh <出来的 html>
```

查六样：字节数没比上一版倒退 · ring 四钮齐 · 每段有 `data-key` · 搜索框在 · 顶部有「产物」标记 · 没有未填占位。
🔴 **字节数倒退是坑 3 的唯一征兆** —— 别只看 stdout 那行 ✅。
最后过一遍 `结构输出标准.md`「🚫 反例」六条。

## 交付

HTML 落项目夹 ＋ 回复里给路径 ＝ 已交付（面板能直接点开）。
🚫 别发预览页 · 🚫 别挂静态服务 · 🚫 没听他说「发」别推站。

## 使用会被记录并推群机器人（作者  要的）

全机 hook `ops/自动化/ai配置推送.py` 在每次 Skill 调用后自动记一行到 `~/.claude/.rs-notify/skill-usage.tsv`，并往 skill 群推（30 分钟内只推一条，压下的攒着下条带出）。🚫 本 skill 不用做任何事、🚫 别再建第二套推送。
看使用记录：`column -t -s $'\t' ~/.claude/.rs-notify/skill-usage.tsv`

## 每个 skill 一页「自述」给作者看（作者  定，本夹是模板）

`自述.md` → `python3 _生成自述HTML.py` → `自述.html`：它是什么、什么时候会动、动了怎么判、不对怎么改。hook 推送的 `📖 文档` 行自动指向 `ops/skill/<名>/自述.html`（存在才指）。
🔴 **改了 SKILL.md 就重出自述页、版本号加一** —— skill 变了页不变就是在骗人。别的 skill 照抄本夹这三份。

## 🔢 版本 ＝ 一轮审阅

**一轮里页面改多少次，版本号都锁死不动。** 他在这一版上提批注 `2.9.01` `2.9.02` ……（递增不复用，解决了也占着号）。

- **改完不升版** —— 只改 md／样式，重出页面，版本号原样。🚫 别顺手 +0.1
- **解决一条就记一句「我改了什么」** —— 他不看过程，但归档时要用，写进批注的 `fixed` 字段或 issue 回复里
- 🔴 **只有他说「这轮排查完毕」才收尾**，四步一次做完：
  1. 从他浏览器读出整轮批注（`bash scripts/读留言.sh`，或 CDP 读 `window.__anno.list`）
  2. 归档进**版本迭代那一栏**：这一版一条，写清这轮解决了哪几号、各改了什么
  3. 清空页面标记（清 `localStorage` 的 `2w2h-anno:<页名>`；发过 GitHub 的议题全部关闭）
  4. **才升下一版**，重出页面
- ⭐ 他的原话：「我的目标是关注对的最后呈现，那些（过程）你留着参考」·「升级动作是我直接给你复盘、发动 skill 去调研」

## 改本夹东西之前

- 改 `build_ring` **签名／参数默认值／注入点语义** ⇒ 通知的是**下游代码**（上面那条现采命令 grep 生成器）；改样式、改骨架内部不用
- 🔴 改**标准内容**（段结构／字数上限／tldr 形态／新增固定段 —— 签名没动但页的长相变了）⇒ 通知的是**所有正在出页的对话线**，🚫 不只是「那次提要求的线」。
  判据：标准变更不报错，在飞的页会**静默停在旧标准**，只有并排一看才发现。改了 tldr 进抬头＋加概念库段，只告诉了 某条对话线，
  某条对话线 同时在出两页 Claude 配置页、按旧标准推上线，成了同组唯一两个旧样式的页，事后返工。
  怎么找「正在出页的线」：`ListAgents` 列出活着的会话 ＋ `tail -20 ~/.claude/.rs-notify/skill-usage.tsv` 看近一天谁调过 2w2h，两边取并集，逐条发消息
- 🔴 改**标准内容**的同一次，还要更新 `scripts/验收.sh`：给新规矩加检查项 ＋ 改开头 `STD_VER`。否则守门的机器停在旧标准 —— 出页跑验收全过、也测不出新规矩。脚本查不了的语义（每段第一句是不是结论、四档判对）验收会列成人工清单，🚫 不假装查过
- `样式.css` `脚本.js` `控件.html` 就是正本，改完重出自述页看效果
- 改了本文件的同一次，`grep` 一遍 `scripts/`（维护办法那条：文档说通了 ≠ 跑手支持）
- 作者在页面上留的批注：`bash scripts/读留言.sh [页名]` —— 🔴 存的是**私有**仓库 `<你的私有仓库>` 的议题（标签「留言」），
  匿名读不到 ⇒ 脚本走 Mac CDP 从他浏览器读（站有登录门）。改完在 issue 上说一句并关掉；⏱ 那把钥匙 **2026-12-04 到期**
- 改完本夹任何东西：重出自述页 → `bash scripts/发布到github.sh "一句话"` 推公开库
