# 格式标准 —— 全机所有 HTML 的统一骨架

🔄 **`260904` 老徐定：整夹从 `~/projects/OPC/格式标准/` 原样搬到 `~/projects/ops/skill/2w2h/`，本文件由 `README.md` 改名而来**
（ops 项目 🚫 不许有 README）。**`~/projects/OPC/格式标准` 现在是一根软链接指到这儿** ——
全机各项目生成器写死的旧路径一处没改、全接住（搬家当天验过字节级一致）。
⭐ **他的计划：等这套完全跑顺了，再把各生成器改成直接路径、撤软链接（几个用时现采，🚫 不写数）。** 在那之前 🚫 别动软链接。
下面正文里凡写「OPC/格式标准」的路径，照旧能用，读的时候心里换成本夹即可。

**`260824` 老徐定两句**：「**我们全部的结构输出都这个模式**」＋
「**我们有且只有一套 黄金圈。一切 html 都用黄金圈结构，老的优化迭代的时候更新，新的就按这个标准。**」

🔴 **这摊管所有项目的 HTML，🚫 不是 OPC 一个项目的事** —— 全局 `~/.claude/CLAUDE.md`
「🎨 写 HTML 的结构标准」那节已点名专线 **`opc-格式标准`**（`260824` 老徐开）⇒
**标准怎么改归这条线**，别的线撞到 HTML 会被全局指到这儿来，🚫 不用追着通知。

🥇 **反过来的代价要记住**：改「必备五件」那种结构性条款会**影响所有项目的存量页**。
老徐已经给了答案（也写进全局了）：**老页迭代时改，🚫 别一次性重写** ——
照「无源 HTML 用时补源」同一条判据：多数页再也不改，提前重写是白干。
⇒ **可以放心加条款，🚫 不用负责回填存量。**

## 正本只有一份

**`结构输出标准.md`** —— 一句话判据 · 必备五件 · 黄金圈四档怎么分 · 机读标记约定 · 反例六条。
🔴 改标准改它，🚫 别在别处写第二份。

---

## 🔴 三层管线 —— 先看这个，别一上手就 `cp 模板.html`

本夹装的不是一份文档，是**三层**。🔑 **分三层的判据是「每层的复用人群不同」**，
合并任何两层都会有人被迫带上不需要的东西。

```
_md2html.py        md → HTML 标签（渲染引擎）        ← 只要渲染的人用这层
    ↓
_出页.py           ＋ 黄金圈骨架（build_ring）        ← 要标准结构的人用这层
    ↓
_出项目说明页.py    ＋ 现算块 ＋ 版本元信息            ← 只有七页项目说明用这层
```

**各层的调用方现算**（🚫 别在这儿手写清单，抄下来第二天就开始烂）：

```bash
cd ~/projects
grep -rl 'from _md2html import' */ --include=*.py | grep -v 格式标准   # 第一层
grep -rl 'from _出页 import'    */ --include=*.py | grep -v 格式标准   # 第二层
grep -rl '_旧设计语言-待退役'    */ --include=*.py                     # 还没迁的老页
```

⚠️ **数的时候当心口径（`260824` 实撞）**：有人报「`_出页.py` 至少五个调用方」，
实测那几个 `ops/skill`／`ops/lwr`／`ops/多AI调用` import 的是 **`_md2html.build`，不是 `build_ring`** ——
它们消费的是**第一层**。⇒ 🔑 **「改 `_md2html` 的影响面」比「改 `build_ring` 的影响面」大，两个数别混。**

🥇 **这次的形状值得单独记住**（报数那条线自己总结的，比数字有用）：
**它报的不是错误的数，是「错误口径下正确的数」** —— 5 在「用格式标准夹里东西的人」这个口径下是对的，
2 在「import `build_ring` 的人」这个口径下是对的。
🔴 **所以这种错不会自己暴露 —— 两边各自都算得没错。** ⇒ 报影响面时**必须把口径一起报出来**，
🚫 光报数字等于没报。

---

## ✍️ 出一个新页，两条路线 —— **默认走 md**

### ✅ 路线 A（默认）：md 正本 → 生成器出页

🔴 **有 md 正本的页，别手写 HTML**（标准里第 4 条）。各项目自己的生成器只剩几行：

```python
import sys; sys.path.insert(0, "/home/ccuser/projects/OPC/格式标准")
from _出页 import build_ring
build_ring(src="xxx.md", dst="xxx.html", eyebrow="ops · 飞牛",
           lead="一句话说这页解决什么", version="v1",
           changelog=[("v1","260824","开张")], gen_rel="ops/飞牛/_生成xxx.py")
```

**照着抄的参考实现**（都验过存在）：

- `~/projects/ops/飞牛/_生成路径清单HTML.py` → `路径清单.html`（本标准第一次实战）
- `~/projects/ops/飞牛/应用-Chrome浏览器/_生成手册HTML.py` → `fygo手册.html`（**最长的一份产物**，带现算注入）
- 七页项目说明：`python3 _出项目说明页.py all`，源在本夹 `项目说明源/<项目>.md`

### ⚠️ 路线 B：`cp 模板.html` —— 只在**没有 md 正本**时用，而且它现在缺三样

```bash
cp ~/projects/OPC/格式标准/模板.html <新页>.html   # 然后把 <!-- 填 --> 换掉
```

🔴 **`260824` 实测的缺陷，用前必须知道**：**页内搜索 · 稳定锚 `key=` · 动态 `--bar`
这三样只在 `_出页.py` 里，`模板.html` 与三个 `_抽出的*` 一样都没有。**

- **不是漏了，是刻意的** —— `_出页.py:105` 写着「🔴 不改 `_抽出的*`，那三份是从样板原样抽的，改了就跟样板分叉」，
  新能力作为 `SEARCH_CSS`／`SEARCH_JS`／`SEARCH_HTML` 常量追加在生成器里。**判据本身成立**
- ⚠️ **但代价落在路线 B 上**：`模板.html` 是从抽出件拼的 ⇒ **手写路线永远拿不到这三样**
- 🥇 **而其中两样是必需件，不是锦上添花** —— 🔴 **理由两层都站得住**：
  - ✅ **技术事实（本线实测，可复验）**：**原生 Ctrl+F 搜不到未展开 `<details>` 里的字**，
    而这套骨架故意把大半内容放进折叠段（`_出页.py:105` 的注释写的就是这条）；
    **`#sN` 是位置编号、插一段后面全漂**，而收藏要的是「回到同一段」
  - ✅ **老徐的用法（`260824` 原话，已有落处可验）** ——
    正本在全局 `~/.claude/CLAUDE.md`「🔑 老徐怎么读这些页 —— 用法决定标准」节：
    ```bash
    grep -n -A10 '老徐怎么读这些页' ~/.claude/CLAUDE.md
    ```
    ⇒ 他的用法是「**反复回到同一段**」，不是「读一遍」⇒ **段长不是缺点**（🚫 别为了短去拆重段）·
    **搜索与稳定锚是必需件** · 引用一律用 `#key`

  🥇 **这条的过程比结论值钱，留着当判据**（`260824` 实撞）：那句话我最初是从跨线消息里拿到的，
  **扫全机零落处** ⇒ 我按「原话与推断分开标」把它降级成「💭 转述待核」，结论则靠上面那层技术事实撑住。
  🔴 **后来证明它确实是原话逐字 —— 病不在核得太严，在那句话被当判据用了却没落成文件。**
  ⇒ ⭐ **判据（已收进 `ops/对齐/标准/迭代 Claude.md` 第 ⑧ 种失效模式）：
  原话只存在于对话里、没有落处 ⇒ 别条线只能当转述打折。**
  🔑 它跟全局那条「发过去要落成文件、🚫 别只丢在聊天里」是同一条，只是对象从**工单**换成了**原话**。
- 📌 **实证对照**：`ops/index.html`（路线 A）搜索框 1 · 稳定锚 11 · 折叠段 22；
  `huoban/对话分工与交接-260817.html`（手写）**14 个折叠段 · 0 搜索框 · 0 稳定锚 ⇒ 那 14 段里的字这页搜不到**

⇒ ⬜ **待办见文末「悬着的」第 1 条。🔑 正解是修 `模板.html`，🚫 不是改全局的措辞** ——
修好之后全局那句「新页一律 `cp 模板.html`」就自动是真的了。

---

## 🔗 归属与下游契约（`260824` 对齐过）

- **正本归 `opc-格式标准`**（本线）—— `_出页.py` 与 `结构输出标准.md` 由本线落笔。
  📌 里面那几样（页内搜索 · 稳定锚 · 动态 bar · changelog 折叠 · `dims=`）是 `ops-fn浏览器` 上一号写的，
  它已声明**今后只提需求、不改那两份** ⇒ **写出处只是为了说明它们不是历史包袱。**
- 🔴 **改 `build_ring` 的接口（签名／参数默认值／注入点语义）要通知下游** ——
  改样式、改骨架内部、改标准正文**不用通知**，那正是复用的意义。
  ⚠️ 下游不会知道自己被改了：它们只是 `import`，**接口一变当场跟着变，且不报错**。
- 🧪 **压力测试页**：`cd ~/projects/ops/飞牛/应用-Chrome浏览器 && python3 _生成手册HTML.py`
  —— 12.6 万字节、四档齐、带现算注入，**骨架改动会不会在长页上塌，拿它试最省事**。

## 样板与「抽出件」

**样板（正本级实例）**：`~/Code/html集合/html-Feynman/课件/为什么是1v1/v2.html`
（线上 `https://feynman2026.pages.dev/课件/为什么是1v1/v2#s1`）

本夹三个 `_抽出的*` 是**从那份样板原样抽出来的**，🚫 别手改。样板变了就重抽：

```bash
cd ~/projects/OPC/格式标准 && python3 - <<'PY'
import re, pathlib
src = pathlib.Path("/home/ccuser/Code/html集合/html-Feynman/课件/为什么是1v1/v2.html").read_text(encoding="utf-8")
pathlib.Path("_抽出的样式.css").write_text(re.search(r'<style>(.*?)</style>', src, re.S).group(1).strip()+"\n", encoding="utf-8")
pathlib.Path("_抽出的脚本.js").write_text(re.search(r'<script>(.*?)</script>', src, re.S).group(1).strip()+"\n", encoding="utf-8")
pathlib.Path("_抽出的工具条.html").write_text(re.search(r'(<div class="bar">.*?</div>\s*</div>)', src, re.S).group(1)+"\n", encoding="utf-8")
PY
```

⚠️ **重抽之后 `模板.html` 要重新拼** —— 🔴 **而现在没有拼它的脚本，只能手工**，
这正是它落后于 `_出页.py` 的机制原因（见路线 B）。

## 🗑 `_旧设计语言-待退役.css` —— 有出口，别当它是第二套风格

灰蓝 `#475569` ＋ 米色那套（原 `ops/页面样式.css`，抽自 `ops/飞牛/飞牛家底.html`）。
🚫 **「另一套可选风格」不存在** —— 新页一律别用它。

- **谁还在用**：就两页，`ops/信息源/小能熊内参/出页.py` · `ops/组网调用/mihomo/出页.py`
  （都是黄金圈立起来之前建的，0 个 `<details>`、自定义版式）
- 🗑 **退役条件**：那两页哪次要改内容，就顺手改走 `_出页.py` 骨架；**改完最后一页 ⇒ 删掉这个 css**
- ⚠️ **故意不现在重写那两页** —— 照「用时补源」：多数页再也不改，提前重写是白干

## 边界 —— 这里管什么、不管什么

- ✅ **管「结构长什么样」**：TL;DR 位置 · 黄金圈四档 · 段落编号与锚点 · 折叠层级 · 工具条 · 触屏尺寸
- 🚫 **不管「代码怎么写」** —— 版本徽章 · changelog · 自包含单文件 · 🚫 圆角＋色条 ·
  代码块尖括号转义，**正本是宪法 12 条**：
  `cat ~/Code/html集合/html-个人ai手册/规范/HTML页面制作规范.html`
- 🚫 **不管「发不发布」** —— 那看各项目自己的约定
  （⚠️ OPC 项目级规矩是**产物一律不进站**，见 `../CLAUDE.md`）
- 🚫 **不改全局 `~/.claude/CLAUDE.md`** —— 那只有 `ops-问题对齐` 那条线能落笔（`260824` Q10）。
  要往全局加／改，落待拍件到 `~/projects/ops/对齐/待拍/<主题>-<日期>.md` 发它，
  体例见那个夹的 `README.md`。✅ **但 `结构输出标准.md` 是本线自己的正本，不受这条约束。**

## 📌 为什么在这儿（`260824` 绕了一圈，最后回到这儿）

**结论：就放 OPC 项目内，夹名 `格式标准`。** 中间错过两次，两次都记下来当判据。

### 🔴 错的第一次：自作主张在 `~/projects/` 顶层建了个夹

老徐说「应该是要前缀加一个 全局，这是全局的方式」，我理解成**新建顶层项目夹**
`~/projects/global-格式标准/` 并给它 `git init` —— 当天被否：
「**你怎么就自己搞了个文件夹，在项目内创建了个文件夹，而且是中文的。
我们正常一个项目的文件夹应该都是英文的。**」

1. **`~/projects/` 顶层跟 `~/.claude/projects/` 的对话桶 1:1 对应** ⇒
   **在那一级新建目录 ＝ 凭空造一个项目**。🚫 别自作主张做这件事
2. ⭐ **顶层必须英文，项目内部的夹本来就是中文** —— 实核：顶层
   `admin biaoda dushu Feynman-OS ft huoban OPC ops sdl xyt`（**一个中文都没有**）；
   而 OPC 内部是 `AI周报`／`个人OKR`／`选题台`，ops 内部是 `飞牛`／`多AI调用`／`研究-工具`
   ⇒ 🔑 **所以 `OPC/格式标准/` 合体例，`projects/global-格式标准/` 不合。**
   ⚠️ 我当时引全局那条「中文目录用 ASCII 前缀-中文」当依据 ——
   **那是允许条款，不是推荐**，而现实体例是顶层无中文。**拿允许条款当依据是错的。**

### 那这份标准归 OPC 还是 ops —— 老徐拍了 OPC

他的话：「**opc 应该是最优解 —— 我的各种思考整理推进都是 opc 范围**」。
⭐ 对得上 OPC 那句总纲：**「让自己的一套标准工作从『知道』做到『做到』」** ——
🔑 `HOW GOOD` 那一档（做到什么程度算好）就是这套东西的产物，第一版正是漏了它。

📌 **他当时也想过 ops**（「毕竟这是机器上设备等以及标准输出格式」）。
两边都沾边是因为这里面本来是两样东西：**「TL;DR ＋ 黄金圈四档」是方法论（像 OPC）**、
**`模板.html`／`_抽出的*`／生成器契约是工程件（像 ops）**。
⇒ 🚫 **但别拆成两处** —— 写一个 HTML 要跳两个夹，代价大于收益。

## ⬜ 悬着的

1. 🔴 **`模板.html` 缺三样能力**（见路线 B）。**建议改法：让 `_出页.py` 反过来生成 `模板.html`** ——
   两条路线就不会再分叉，且**不动 `build_ring` 接口 ⇒ 下游零影响**，也不用改全局措辞。
   ⚠️ 归属曾一度不清（`ops-fn浏览器` 已退出那摊、全局点名本线）⇒ **现在归本线，等老徐一句话就能做**
2. ⬜ **`dims=` 三维度标签**（成熟度 1-5 播种→常青 · 重要程度 1-5 星 · 类型）——
   `_出页.py` 支持了，但**要不要推到别的页没定**（老徐在 fygo 那摊要的）。七页项目说明没挂，那判断是对的
3. 🔴 **OPC 不是 git 仓库**（`~/projects/` 下只有 `dushu`／`ops`／`sdl` 是）⇒
   本夹的模板与抽出件、以及三层生成器**改坏了没版本可退**，而它们已被多条线反复调用。
   **要不要给 OPC `git init`，归 OPC 那条线或老徐定**，🚫 别的线别替它决定
4. ⬜ **那两页退役**（见旧设计语言那节）
5. ⬜ **`结构输出标准.md` 有两处该跟本文件对齐**（那份也归本线，但改它要老徐一句话）：
   - **「怎么用」那节第 1 条还写着「复制 `模板.html` 改名」** —— 跟上面「默认走 md」的排序反了
   - 🔑 **该钉死一句措辞：「档位恒 4，`.ring` 按钮可以多于 4」** ——
     `260824` 实撞：隔壁自报 `fygo手册.html` 是「黄金圈 **5 档**」，跟标准的「四档」字面打架。
     实测**没有第五档**，是 `w2`(HOW) 出现两次、**一档横跨两段**（标准明文允许，
     `_出页.py` 里那条配色修复注释正是为此写的）。
     ⚠️ 但按「5 档」的说法去建页，会有人去找不存在的第五个档位
     ⇒ 照失效模式⑤「措辞差一个词，判据就换方向」，**这句得钉在正本里。**

6. ⬜ **页顶显示最新一条 changelog**（`260905` 老徐看 `/cdp/` 页提：「tldr 上面写入最新的一次更新了什么，更好看」，`ops-cdp` 转来）——
   建议加在 `build_ring` 里、所有页重出自动有；样式跟版本徽章一族，一行 `🔄 vN · 日期 · 改了什么`，长了截并 ↓ 跳底部。
   🔴 **老徐 `260905` 说：他先搞定 cdp 和 wxfx 两条对话再回来专门聊 2w2h，别的线发来的先记录不动** ⇒ 等他回来一句话再做
7. ⬜ **让任何项目一说「出页」就自动调 2w2h**（我 `260904` 晚问他的第一步，他还没答）—— 他定的方向：黄金圈 HTML 走 skill 不走 rules，目标是 2w2h 成全局标准
8. 📌 `260905` `ops-cdp` 报：推送脚本 `THROTTLE_SEC["rule"]=0`（rule／hooks 变动不节流）、`--used` 独立 `"used": 600` 档，skill 30 分未动。只是知会，无事要做

## 谁在用它

- **全局 `~/.claude/CLAUDE.md`**「🎨 写 HTML 的结构标准」那节 —— 指过来，🚫 没写第二份
- 🚫 **别在这儿手写「谁在用」的名单** —— 会过期，现采：
  ```bash
  grep -rln "OPC/格式标准" ~/.claude/CLAUDE.md ~/projects/*/ 2>/dev/null | grep -v '/.git/'
  ```

⚠️ **本夹没有对话线常驻在目录里** —— 谁要改就从自己那条线伸手过来。
🚫 别在这儿开对话线（开了它就变成又一个项目桶）。

## 📅 沿革

- **`260905` 样式与脚本以本夹为正本** —— 老徐一段段聊完抬头与功能栏后拍「最后你改的才是标准」。三份从 Feynman 样板抽出来的文件改名 `样式.css` `脚本.js` `控件.html`，不再重抽；下面凡说「重抽」的都是旧事

- **`260824` 重写本文件（当时还叫 README.md）** —— 原来只写了「`cp 模板.html`」＋三个 `_抽出的*`，
  **`_出页.py`／`_出项目说明页.py`／`项目说明源/` 整个三层管线在门面里没出现** ⇒
  照 README 上手的人会去手写 HTML，而标准第 4 条明说「有 md 正本就别手写」。
  🔑 **这是门面落后于实现**（`ops/对齐/标准/迭代 Claude.md` 第③条第①种失效模式：
  当时对、后来环境变了而文件没跟上）。同时补：三层调用方口径 · 模板缺三样 · 归属与下游契约 ·
  全局背书 · 旧设计语言退役条件。**原件备份 `/tmp/README.bak-260824`。**
