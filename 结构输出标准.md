# 结构输出标准 —— 我们所有 HTML 的统一骨架

📍 **真身在 `~/projects/ops/skill/2w2h/`**（`260904` 从 `OPC/格式标准/` 搬来；旧路径是软链接，跑顺前别动）。出页流程见同夹 `SKILL.md`。

**老徐 `260824` 定：「我们全部的结构输出都这个模式。」**
📌 **参照实例（正本级样板）**：`~/Code/html集合/html-Feynman/课件/为什么是1v1/v2.html`
（线上 `https://feynman2026.pages.dev/课件/为什么是1v1/v2`）——
**样式与脚本就是从它原样抽出来的**，🚫 别另起一套。

🔑 **本文件管「结构长什么样」。**「代码怎么写」那一层（版本徽章 · changelog · 自包含单文件 ·
🚫 不用圆角＋色条 · 代码块尖括号必须转义）**正本是宪法 12 条**：
`cat ~/Code/html集合/html-个人ai手册/规范/HTML页面制作规范.html` —— 🚫 本文件不写第二份。

## 一句话判据

**读者在展开任何一节之前，就该看懂：这页要说什么、按什么顺序说、每节属于哪一档。**
⇒ **把「重要程度」和「讲述顺序」编码进排版本身**，而不是让读者自己读完再总结。

## 必备五件 —— 缺一件就不算这个模式

### ① 第 1 段固定是 TL;DR：`🎯 先看三句话`

🔄 **`260905` 老徐定：三行进抬头，不再是折叠段** —— 页面顺序固定为 **标题 → 一句话 → 三行 → 功能栏 → 黄金圈四钮 → 各段**。
md 里仍写 `tldr` 段，出页器把它渲染进抬头；折叠段从 WHY 起编号。**字数上限：标题 ≤ 16 · 一句话 ≤ 30 · 三行每行 ≤ 20**（超了 stderr 报，不截断）。
三行各答一个问题（它是什么 · 什么时候动 · 不对怎么改），🚫 跟一句话重复、🚫 互相重复。

- 用 `ol.three`（带圆圈编号的三条），**三条就是三条**，🚫 别写五条
- 每条正文一句话结论，`<em>` 里补一句「凭什么／在哪展开」
- ⭐ 判据：**只读这一段就能把这页的结论讲给别人听。**

### ② 紧接一个「黄金圈导航」`.ring` —— 四档，🚫 不是三档

四个按钮，各跳到对应的段（`data-go="s2"`）。⭐ **顺序是「从里往外」**：

- **`WHY`**（`.tag.why`，橙 `--why`）—— **为什么要管这件事**。🔴 不是功能清单，是「不做会怎样／不可替代在哪」
- **`HOW`**（`.tag.how`，紫 `--how`）—— **凭什么做到**（机制／原理／它靠什么做到）
- **`WHAT`**（`.tag.what`，青 `--what`）—— **具体是什么／有哪些**（清单、选项、参数、命令）
- **`HOW GOOD`**（`.tag.good`，玫红 `--good`）—— 🔑 **做到什么程度算好** ——
  验收标准、今晚就能落地的最小一步、怎么自检。**这一档最容易被漏掉，但它是"从知道到做到"的那一步。**

每个按钮三行：`.k`（`WHY · 第 2 段`）· `.t`（标题）· `.d`（一句钩子）。
下面跟一句 `.ringnote` 说明讲法（例：「这一页从里往外讲：先为什么，再凭什么，最后才是怎么做」）。

⚠️ **四档不必一段对一段** —— 一档可以横跨两段，也可以某段不挂档（如 TL;DR 段、参考文献段不挂）。
🔴 但 **`.ring` 里四个按钮必须齐**，缺哪档就说明那部分没想清楚。

### ③ 每段一个 `details.sec`，可锚点直达

```
<details class="sec" id="s2">
  <summary><span class="num">2</span><span class="tag why">WHY</span>
           <span class="ti">段标题</span><span class="sub">副标：一句话说这段讲什么</span></summary>
  <div class="body">
    <p class="lead2">先给结论：<strong>……</strong></p>
    ……
  </div>
</details>
```

- 🔴 **`id="s1" s2 s3…` 连续编号** —— 给**导航**用（翻页 · 进度 · `.ring` 的「第 N 段」）
- 🥇 **另外给每段一个稳定锚 `key=`** —— 给**寻址**用（收藏、互相引用）

  🔴 **`260824` 实测出来的缺陷，必须两个都有**：`id="sN"` 是**位置编号**，
  插一段后面全漂 —— 当天实证：在第 2 位插一段，**`#s6` 从「怎么维护」漂到「有什么、怎么用」**，
  而 **`#维护` 仍然指同一段**。
  ⚠️ **而且这类错完全静默** —— 链接照样能跳，只是跳到了错的段。

  🔑 **病因不是"编号会漂"，是让同一个 `id` 担了两个职责** ——
  **导航必须是位置 · 寻址必须是身份**。⭐ **一个东西担两个职责，必然在其中一个上出错。**

  ```
  ## 怎么维护、怎么发现问题
  <!-- sec tag=HOW-GOOD sub=… ring=…|… key=维护 -->
  ```

  - **key 取短、稳、不跟着标题改的词** —— 标题改了锚不该断
  - 🚫 **故意不做「标题自动 slug 兜底」** —— 标题也会改，那只是把「会漂」换成「会断」，
    等于造出第二个静默失效的东西。**没写 key 就没有锚**，生成器往 stderr 报，让人补
  - ✅ **旧的 `#sN` 链接不断**（向后兼容），两种锚**行为一致**（都会自动展开祖先 `details`）
    ⚠️ `260824` 实测抓到过一次不一致：原脚本的 hash 处理**只在加载时跑、没监听 `hashchange`**
    ⇒ 页面打开后点站内 `#sN` 没反应，而语义锚有 ⇒ 🔴 **「有的锚能用有的不能」比「都不能用」更坑**，已统一
- 🔴 **`.body` 第一句必须是 `p.lead2` 的结论** —— 展开后第一眼就是答案，🚫 别先铺垫
- `.num` 编号 · `.tag` 黄金圈档位 · `.ti` 标题 · `.sub` 副标，四样都要
- 二级用 `details.sub`（`.no` 编号 ＋ `.nm` 名称 ＋ `.en` 英文/原文）

### ④ 右侧竖排控件条 `.ctl`（`260905` 老徐定，取代原来整条吸顶工具条；同日改竖排，为后面加按钮留位）

一条固定在右侧的竖条，只放图标，从上到下四组、组间细线隔开 —— 导航（☰ 目录 · ▲ 段号 ▼）· 展收（⊞ ⊟）· 阅读（🔍 · A＋ A－）· 互动（💬 留言）。原文：☰ 侧边目录（宽屏默认常驻左侧、可收起并记住；窄屏是抽屉）· 🔍 搜本页（点开才出输入框，`/` 也行）· A－ A＋ 缩放（每级 10%，80–160%，浏览器记住）· ‹ › 上下段 · ⊟ ⊞ 全收全展 · 小字 `1/5` 段号。
上一段下一段不再是文字按钮，靠 ‹ › 和 ← → 键。手机上同一个胶囊贴右上。

📌 下面这节是它的前身，留着说明为什么当初长那样：

#### （旧）sticky 工具条 `.bar`

📌 **搜索框、动态 `--bar` 由骨架自动加**（见下面「骨架自带的四样」），作者不用管。

上一段／下一段／全展开／全收起 ＋ `第 N / M 段` 进度 ＋ 提示「← → 键可翻」。
🔑 脚本已经写好（`_抽出的脚本.js`）：支持键盘翻页 · hash 直达 · 「阅读模式」（全展开后翻页不收起）·
当前段高亮 `.cur` · 自动 `history.replaceState` 更新锚点。
🔴 **`--bar:64px` 配合 `scroll-margin-top:calc(var(--bar) + 14px)`** —— 否则 sticky 条会挡住锚点跳过去的标题。

### ⑤ 最后一段是 `📎 对照读物 · 不是作业`

正文里的引用写 `<sup><a href="#r2">[2]</a></sup>`，末段列出来源并给 `id="r2"` 锚点。
⭐ **「不是作业」这个措辞是刻意的** —— 给延伸不给负担。

## 🤖 骨架自带的四样（作者不用管，但**不是锦上添花**）

📌 `260905` 又加两样：**💬 批注**（像 Word：选中文字冒气泡／右键 → 黄线 ＋ 右侧对齐卡片 → 🔴 解决即从页面消失（原文常已被改掉，锚不住）、回看在 💬 列表的「已解决」折叠里；先存浏览器，「发到 GitHub」点了才出去；锚定＝段 key ＋ exact ＋ 前后 32 字）· **侧边目录**（参考飞书／语雀）—— 段与小节从 DOM 现算，点哪跳哪、当前段高亮；多段内容不用翻就看得见全部框架。

⚠️ 这四样由共用出页器 `_出页.py` 自动加，**作者写 md 时不用做任何事** ——
所以它们不在上面的「必备五件」里（那五件是**作者要做的**）。
🔴 **但别把它们当装饰** —— 下面每一条都有"缺了就不成立"的理由。

### 1. 🥇 页内搜索（缺了这页就搜不了）

顶部工具条里的搜索框：输入词 → 全页高亮 → `↑`／`↓` 逐个跳 → 计数 `3/83`；`/` 聚焦、`Esc` 清除。

🔴 **它为什么是必需的**：**浏览器原生 Ctrl+F 搜不到没展开的 `<details>` 里的内容**，
而这套骨架**故意把绝大部分内容放进折叠段**。
⇒ ⭐ **`260824` 两条线各自撞到同一件事**：`fygo手册` 39 个折叠块 · 七条线的项目说明页每页约 8000 字
且大半在折叠段里 ⇒ **没有页内搜索，等于这页搜不了。**

🥇 **它的核心不是"高亮"，是「跳过去之前把祖先 `<details>` 全打开」** ——
实测搜 `CDP` 命中 83 处，跳转过程中 39 个折叠块里 **34 个被自动展开**，
且每次跳转后当前命中点 `getClientRects()` 非空（**真看得见，不是滚到一个收起的盒子上**）。

### 2. 缩放件（`260905` 换进来的；原来这位是「动态 `--bar`」）

宪法 12 条要的 150% 缩放件，现在在右上角胶囊里：A－ A＋，只缩正文 `#page`，胶囊自己不变，记进浏览器。
⚠️ 原来这一条讲「`--bar` 实测高度写回 CSS 变量」—— 吸顶大条撤了，这事没了，`--bar` 现在是 6px 常量。留段旧文如下：

#### （旧）动态 `--bar`

🔴 `--bar` 原来在样式里**写死 64px**，而工具条加了搜索框后会**换行变高**（实测 109px）⇒
`details` 的 `scroll-margin-top` 按 64px 算 ⇒ **锚点跳过去被挡 45px**。
⇒ 改成 **JS 实测高度写回 CSS 变量**（`ResizeObserver` 盯着），几行都不怕。
📌 判是否生效：页面里跑 `__barHeight()`，两个值应相等。

### 3. changelog 折叠

底部版本迭代默认收起（`details.cl`）—— 版本一多就会把页尾撑得比正文还长。

### 4. ⬜ 三维度标签（`dims=`，**可选**）

成熟度 1-5（`播种 · 萌芽 · 成长 · 稳定 · 常青`）· 重要程度 1-5 星 · 类型 · 评级依据。

```python
build_ring(..., dims={"maturity": 4, "importance": 5, "kind": "软件 ＋ 应用案例",
                      "note": "为什么不是 5 —— 一句话"})
```

🔴 **这一样是可选的，🚫 别自动给所有页挂** —— `260824` 它是老徐在 fygo 那摊提的需求，
**要不要推到别的页得他定**（`ops-问题对齐` 那边就明确没给项目说明页挂，判断是对的）。
⭐ 挂的时候**诚实评，🚫 别自己给自己打满分** —— `note` 里写清"为什么不是 5"。

---

## 触屏与配色（都在抽出的样式里，照用即可）

- **点击目标最小高度**：`.bar button` 44px · `details.sub > summary` 48px · `details.sec > summary` 52px
- **主色** teal `--acc:#0f766e` · **纸色底** `--bg:#faf9f4` · 卡片 `--card:#fff`
- **黄金圈四色** `--why:#b45309`（橙）· `--how:#7c3aed`（紫）· `--what:#0f766e`（青）· `--good:#be185d`（玫红）
- **折叠指示** `＋`／`－` 用 `summary::after`，🚫 别用系统三角
- 正文 `14.5px` · 行高 `1.78` · 主区 `max-width:780px`

## 🚫 反例（这几样就是"结构化不够"）

- 🚫 **一堆同级 `<h2>` 平铺** —— 信息都在但没有层级，读者得自己找哪块重要
- 🚫 **按「我发现的顺序」排版** —— 那是过程不是结构
- 🚫 **TL;DR 放最后**（或者干脆没有）
- 🚫 **只有 Why/How/What 三档，漏掉 HOW GOOD** —— 少了验收，读者不知道「算做到了没」
- 🚫 **段落没有 `id`** —— 没法指到具体一段
- 🚫 **展开后先铺垫再给结论**

## 🤖 从 md 生成（推荐做法）—— 机读标记约定

🔴 **有 md 正本的页，别手写 HTML** ⇒ 在 md 的 `## 标题` **下一行**放一行机读标记，生成器照它拼骨架：

```
## 🎯 先看三句话
<!-- sec tldr sub=整页就讲这一件事 -->

## 为什么需要它
<!-- sec tag=WHY sub=副标一句 ring=按钮标题|一句钩子 -->

## 📎 对照读物 · 不是作业
<!-- sec sub=各摊正本在哪 -->
```

- **`tldr`** —— 这段是第 1 段 TL;DR。段内的有序列表按 **`‖`** 切成「主句 ＋ `<em>` 补充」，渲染成 `ol.three`
- **`tag=`** —— 四档之一：`WHY` · `HOW` · `WHAT` · `HOW-GOOD`（渲染成 `.tag.why/.how/.what/.good`）
- **`ring=`** —— 进 `.ring` 导航，格式 `按钮标题|一句钩子`；省略则不进导航
- **`sub=`** —— summary 里的副标
- 三样都省 ⇒ 该段不挂档、不进导航（TL;DR 段和末段就该这样）

⭐ **生成器该做三件事**：① 复用项目现成的 md 渲染器（🚫 别写第二份 md 解析）·
② 样式脚本从本夹 `_抽出的*` 读（🚫 别在生成器里写死 CSS，否则样板一变就分叉）·
③ 🔑 **四档缺哪档就往 stderr 报警** —— 缺档说明那部分没想清楚。

### 🔢 会变的东西用「现算注入」，🚫 别写死在 md 里

md 里放占位，生成器出页时算好塞进去：

```
<!-- inject:tree -->
```

```python
build_ring(..., inject={"tree": 现算出来的HTML片段})
```

⭐ **判据（全局 CLAUDE.md 的老规矩）**：**手写的清单抄下来第二天就开始烂，而且烂得没有声音。**
⇒ 文件清单 · 数量 · 体积 · 行数 · 谁在用它 —— 一律现算。
🔴 **占位没被填会往 stderr 报警**（`未填的注入占位`），别让它静默留在页面里。
📌 **实例**：`~/projects/ops/飞牛/应用-Chrome浏览器/_生成手册HTML.py` 的「文件目录」那段 ——
扫夹现算文件名/大小/行数，**说明文字从生成器里的映射表取**；
⭐ 出现未登记的新文件会标 `⬜ 未登记`，逼人补一句说明。

📌 **参考实现**（照着抄）：`~/projects/ops/飞牛/_生成路径清单HTML.py`
⇒ 产物 `~/projects/ops/飞牛/路径清单.html`（本标准的第一次实战，14 项自检全过）。

## 怎么用

1. 复制 `模板.html` 改名，把 `<!-- 填 -->` 的地方换掉
2. 段数不是四段就四段 —— **`.ring` 四档齐** ＋ 段落 `id` 连续就行
3. 出完先自检本文件「反例」六条
4. 🔑 **如果内容有 md 正本**（多数情况该有），🚫 别手改 HTML ——
   照各项目的生成器改 md 再重出（例：`~/projects/ops/飞牛/应用-Chrome浏览器/_生成报告HTML.py`）

📌 **`260905` 起：`样式.css` `脚本.js` `控件.html` 就是正本**（老徐「最后你改的才是标准」），不再从 Feynman 那个 v2.html 样板重抽。要改就改这三份，改完重出自述页看效果。
